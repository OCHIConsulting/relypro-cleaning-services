# Phase 1: capture and measurement

## Completion status (13 August 2026)

Locally complete: provider-neutral lead schema and endpoint; client/server validation; spam timing and honeypot checks; distributed hashed-IP rate limiting; retry deduplication; public references; session draft recovery; explicit failure/fallback states; optional customer-controlled WhatsApp handoff; analytics parameter allow-list; privacy acknowledgement; CRM mapping; human-approved messages; commercial and Airbnb checklists; tests and responsive/accessibility review.

Integration-ready, not active: a production destination, Cloudflare Pages execution, KV binding, HubSpot field creation/pipeline configuration, monitoring and processor disclosure. The Cloudflare account is signed in but still requires email verification before a project can be created. No DNS, customer data, message, publication or deployment was changed.

Verification: `npm test` builds 28 pages and passes 21 tests. Chrome checks at 1440 x 900 and 390 x 844 found no horizontal overflow; both quote/contact forms have one H1, no duplicate IDs, no visible unlabeled controls, and alert semantics for server errors. With no consent, and after choosing essential-only, no Google Tag Manager script loaded. A synthetic unavailable-endpoint submission retained the visible entries, stayed on the form URL, showed no false success, and exposed three fallback routes.

## Architecture decision

The public site is generated by Eleventy and deployed by GitHub Actions to GitHub Pages. GitHub Pages cannot execute a server endpoint. The repository therefore includes a provider-neutral Cloudflare Pages Function at `functions/api/leads.js`; it is not active on the current host and must not be described as live until hosting and the destination are approved.

The browser posts first-party JSON to the endpoint configured by the `relypro-lead-endpoint` meta tag. The endpoint validates and normalises input, rejects the honeypot and implausibly fast submissions, applies a ten-minute rate limit using a salted hash of the source IP in KV, calculates a stable idempotency key, checks a 24-hour KV record when bound, generates a public enquiry reference, and only returns success after the configured destination accepts the record.

The storage boundary is deliberately provider-neutral: `LEAD_DESTINATION_URL` receives the documented JSON over HTTPS, with an optional bearer token from `LEAD_DESTINATION_TOKEN`. A small adapter or managed workflow can translate this payload to HubSpot without putting a secret in the browser.

## Current journey and failure points

Before this change, quote/contact forms only opened WhatsApp. Closing the tab, popup blocking, or not pressing Send meant the enquiry was lost, while the page displayed a handoff message rather than a saved-lead confirmation.

The new journey is: form entry -> client validation -> first-party POST -> server validation -> destination acceptance -> public reference -> optional WhatsApp handoff. On any integration failure there is no false success: fields remain in the DOM, a session-only draft supports reload recovery, and phone/email/WhatsApp fallbacks are shown. A retry reuses the same browser submission ID, enabling deduplication.

## Lead API contract

`POST /api/leads`, JSON only. Required quote fields are: `kind`, `name`, `contact_method`, `contact_details`, canonical `service`, UK `postcode`, `property_summary`, `privacy_acknowledged`, `started_at`, `client_submission_id`, and `landing_page`. `preferred_date` and the three non-personal attribution fields (`utm_source`, `utm_medium`, `utm_campaign`) are optional. The hidden `website` field must remain empty.

Responses:

- `200 { ok: true, reference, duplicate? }` only after persistence or a confirmed duplicate.
- `422 { error: "validation_failed", fields }` for field errors.
- `429 { error: "rate_limited" }` after more than five requests per IP in a ten-minute KV-backed window.
- `503 { error: "unavailable" | "upstream_failure" }` when storage is unconfigured or fails.

The rate-limit key is a salted SHA-256 digest; no raw IP address is written to KV, and each counter expires after ten minutes. KV rate limiting and deduplication are best-effort because KV is eventually consistent; for strict exactly-once creation, the destination must also enforce uniqueness on `client_submission_id` or the calculated idempotency value.

## Analytics data contract

Permitted events: `quote_start`, `quote_submit_attempt`, `lead_capture_success`, `lead_capture_failure`, `whatsapp_handoff`, `phone_click`, and `email_click`. Supporting UI events may include `form_start`, `service_selected`, and `quote_click`.

Permitted parameters are page path, form name, canonical service, non-personal UTM source/medium/campaign, consent state, and a small error class. Never send a reference, name, contact detail, postcode, property summary, message, link text, full `mailto:`/WhatsApp URL, `gclid`, or form value. Analytics loads and events reach GA4 only after valid consent; the local diagnostic event remains available for tests without personal data.

## Environment and production setup checklist

1. Verify `admin@relypro.co.uk`, then create a Git-integrated Cloudflare Pages project for `OCHIConsulting/relypro-cleaning-services` with build command `npm run build`, output `_site`, and production branch `main`.
2. Create the chosen lead destination (HubSpot adapter/workflow recommended) and verify its data-processing terms and retention configuration.
3. Add `LEAD_DESTINATION_URL`, `RATE_LIMIT_SALT`, and, if required, `LEAD_DESTINATION_TOKEN` to the host's encrypted secret store. Never add values to Git.
4. Create and bind a KV namespace as `LEAD_DEDUPLICATION`; use separate preview and production namespaces, and set a destination-side unique property for the submission ID as the final duplicate guard.
5. If the API is on another origin, update the page meta tag, CSP `connect-src`, endpoint CORS allow-list, and preflight handling together.
6. Add the approved processor and purpose to the privacy notice before activation.
7. Configure alerting on sustained 5xx responses and a dead-letter/retry process that never logs request bodies; optionally add a Cloudflare rate-limit rule as defence in depth.
8. Run synthetic acceptance tests, inspect the destination record, delete the synthetic record, and obtain deployment approval.

## Rollback

Revert the Phase 1 commit or restore `assets/js/main.js`, `get-quote.html`, `contact.html`, `privacy.html`, and `cookies.html`; remove `functions/`, `lib/lead-core.js`, Phase 1 tests/docs, and the example vars file. No production data migration is needed until a destination has been activated. If rolling back after activation, disable the endpoint secret/binding first and export or retain lead records according to the privacy notice rather than deleting them blindly.
